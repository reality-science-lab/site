# build output
dist/
.astro/

# deps
node_modules/

# WordPress dump (build-time input only — DO NOT COMMIT, 2.77GB archive / 116MB sql)
_dump/

# scratch / crawl caches
.cache/
*.log

# RSI LP is published into the site at build time (scripts/embed-lp.mjs) from
# institute/apps/lp/ — the generated copy must not be duplicated in git.
/public/institute/

# os
.DS_Store

# large local archives, not for git
*.zip

# institute/ = 現実科学研究所 (RSI) 資材（docs + LP）をこの repo に統合管理。
# 注意: docs にローンチ前資料を含むが、本 repo は public（ユーザー判断で当面 public 運用）。
